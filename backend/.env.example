# Local default can stay on SQLite.
# Swap to PostgreSQL when you explicitly want a Postgres-backed environment.
DATABASE_URL=sqlite:///./routefusion.db
JWT_SECRET=routefusion-demo-secret
JWT_ACCESS_EXP_MINUTES=30
JWT_REFRESH_EXP_DAYS=14
CORS_ORIGINS=http://localhost:5173
